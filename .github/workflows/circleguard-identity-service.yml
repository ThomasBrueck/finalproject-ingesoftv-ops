name: "CI/CD — circleguard-identity-service"

on:
  workflow_dispatch:
    inputs:
      dev-branch:
        description: 'Branch del repo de desarrollo a construir'
        required: false
        default: master

jobs:
  pipeline:
    uses: ./.github/workflows/_cicd-pipeline.yml
    with:
      service-name: circleguard-identity-service
      service-port: '8083'
      dev-branch: ${{ inputs.dev-branch || 'master' }}
      run-cd: true
    secrets: inherit
