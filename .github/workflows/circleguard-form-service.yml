name: "CI/CD — circleguard-form-service"

on:
  workflow_dispatch:
    inputs:
      dev-branch:
        description: 'Branch del repo de desarrollo a construir'
        required: false
        default: master

jobs:
  pipeline:
    uses: ./.github/workflows/_cicd-pipeline.yml
    with:
      service-name: circleguard-form-service
      service-port: '8086'
      dev-branch: ${{ inputs.dev-branch || 'master' }}
      run-cd: true
    secrets: inherit
